/**
 * PinnedMessagesSection — Settings section for managing pinned messages.
 *
 * Displays:
 *  - Agent filter
 *  - Pinned messages list with actions (view conversation, unpin)
 */

import { PinnedMessages } from '@/pages/Knowledge/PinnedMessages'

export function PinnedMessagesSection() {
  return <PinnedMessages />
}
