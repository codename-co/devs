/**
 * Files — Thin wrapper that re-exports FilesSection from components/.
 * Kept for backward-compatibility with existing imports.
 */

import { FilesSection } from './components'

export const Files = FilesSection
