import { Container, Section, Widget } from '@/components'
import Layout from '@/layouts/Default'

<Layout header={{
    color: 'bg-default-50',
    title: 'Demo',
    subtitle:
      'This page demonstrates the rendering of specialized code blocks within markdown content'
  }}>
  <Section>
    <Container>

## Standard Code Block

Here is a standard JavaScript code block:

<Widget type="code" code={
  /* JS */
  `function greet(name) {
  return \`Hello, \${name}!\`
}`} />

## ABC Music Notation

The following is an example of ABC music notation, which should be rendered as a musical sheet.

<Widget type="abc" code={
  /* abc */
  `X:1
T:The Legacy Jig
M:6/8
L:1/8
R:jig
K:G
GFG BAB | gfg d2B | GFG BAB | d2B G2B |
GFG BAB | gfg d2B | c2c B2A | GFG B2G :|`}
/>

## SVG Graphics

This is an example of an SVG graphic embedded directly in the markdown.

<Widget type="svg" code={
  /* svg */
  `<svg width="100" height="100" viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg">
  <circle cx="50" cy="50" r="45" fill="#3498db" stroke="#2980b9" stroke-width="5"/>
  <rect x="35" y="35" width="30" height="30" fill="#ffffff"/>
</svg>`}
/>

## Mermaid Diagram

This is a Mermaid diagram that illustrates a simple flowchart.

<Widget type="diagram" code={
  /* mermaid */
  `graph TD;
    A[Start] --> B{Is it working?};
    B -- Yes --> C[Great!];
    B -- No --> D[Fix it];
    D --> B;
    C --> E[End];
    E --> A;`}
/>

<Widget type="diagram" code={
  /* mermaid */
  `sequenceDiagram
    participant A as Alice
    participant B as Bob
    A->>B: Hello Bob!
    B-->>A: Hello Alice!
    A->>B: How are you?
    B-->>A: I'm good, thanks!`}
/>

    </Container>
  </Section>
</Layout>
