import { Link } from 'react-router-dom'
import {
  Container,
  MarkdownRenderer,
  Section,
  Title,
  Widget,
} from '@/components'
import Layout from '@/layouts/Default'
import ABC from './abc.mdx'
import Code from './code.mdx'
import Diagram from './diagram.mdx'
import Html from './html.mdx'
import markdown from './markdown.md?raw'
import Pptx from './pptx.mdx'
import Presentation from './presentation.mdx'
import Svg from './svg.mdx'

<Layout
  header={{
    color: 'bg-default-50',
    title: 'Demo',
    subtitle:
      'This page demonstrates the rendering of specialized code blocks within markdown content',
  }}
>
  <Section>
    <Container>
      <Title level={2}>Widgets</Title>
      <Pptx />
      <Presentation />
      <ABC />
      <Diagram />
      <Html />
      <Svg />
      <Code />
    </Container>
  </Section>
  <Section>
    <Container>
      <Title level={2}>Markdown</Title>
      <MarkdownRenderer
        content={markdown}
        className="prose dark:prose-invert prose-sm"
      />
    </Container>
  </Section>
  <Section>
    <Container>
      <Title level={2}>More Demos</Title>
      <div className="flex flex-col gap-2">
        <Link to="/demo/timeline" className="text-primary hover:underline">
          Task Timeline — Multi-agent execution with HITL
        </Link>
        <Link to="/demo/conversations" className="text-primary hover:underline">
          Conversation Tests — Live LLM test suite
        </Link>
        <Link to="/demo/code" className="text-primary hover:underline">
          Code Sandbox — Sandboxed code execution
        </Link>
      </div>
    </Container>
  </Section>
</Layout>
